# Publishable — safe in the browser bundle.
NEXT_PUBLIC_SANITY_PROJECT_ID=byz529pi
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_STUDIO_URL=http://localhost:3333

# Secret — server-only. Create a Viewer token at https://www.sanity.io/manage
# (project byz529pi → API → Tokens). Required for draft mode and the
# Presentation tool; published content renders fine without it.
SANITY_API_READ_TOKEN=
